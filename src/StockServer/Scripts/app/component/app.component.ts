import { Component, OnInit, Input } from "@angular/core";


@Component({
    selector: "stockserver",
    templateUrl: "./app/view/app.template.html"

})

export class AppComponent {
    title = "Stock Server"
}