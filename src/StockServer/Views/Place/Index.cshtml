@model IEnumerable<StockServer.Models.Common.PlaceViewModel>

@{
    ViewData["Title"] = "Places";
}

<h2>Places</h2>

<p>
    <a asp-action="Create" asp-area="">Create New</a>
</p>

<ul class="list-group">
    @foreach(var item in Model)
    {
        <li class="list-group-item">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-xs-1">
                        <div class="image">
                            <img src="@item.LogoUrl" style="width:30px;height:30px" />
                        </div>
                    </div>
                    <div class="col-xs-8">
                        <div class="text-left">
                            <strong>@item.Name</strong>
                        </div>
                    </div>
                    <div class="col-xs-3">
                        <div class="text-right button">
                            <a asp-action="PlaceOffers" asp-area="" asp-controller="offer" asp-route-id="@item.Id">
                                <span class="glyphicon glyphicon-gift" aria-hidden="true" style="font-size:20px"></span>
                            </a>
                            <a asp-action="PlacePurchase" asp-area="" asp-controller="offer" asp-route-id="@item.Id">
                                <span class="glyphicon glyphicon-export" aria-hidden="true" style="font-size:20px"></span>
                            </a>
                            <a asp-action="Update" asp-area="" asp-controller="Place" asp-route-id="@item.Id">
                                <span class="glyphicon glyphicon-pencil" aria-hidden="true" style="font-size:20px"></span>
                            </a>
                        </div>
                    </div>

                </div>
            </div>
        </li>

    }
</ul>
<!--<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Latitude)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Longitude)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contact)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Latitude)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Longitude)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contact)
            </td>
            <td>
                <p>
                    <a asp-action="PlaceOffers" asp-area="" asp-controller="offer" asp-route-id="@item.Id">Offers</a>
                </p>
                <p>
                    <a asp-action="PlacePurchase" asp-area="" asp-controller="offer" asp-route-id="@item.Id">Purchase</a>
                </p>
            </td>
            
        </tr>
}
    </tbody>
</table>-->
